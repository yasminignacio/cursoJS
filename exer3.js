// 1. Defina uma variável para a idade
const idade = 20;
    // 2. Crie a estrutura condicional `if...else`
    if (idade >= 18) {
        // Este bloco de código é executado se a condição (idade >= 18) for verdadeira
        console.log("Você pode comprar bebida alcoólica. Aproveite com responsabilidade!");
    } else {
        // Este bloco é executado se a condição for falsa
        console.log("Desculpe, você não tem idade para comprar bebidas alcoólocas.");
    }